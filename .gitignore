# don't track sp-include -- get these from SourceMod directly
sp-include/

# these are generated files
keywords/generated/
*.elc

# distfiles
sourcepawn-mode-*.tar.gz
sourcepawn-mode-*.tar.bz2
sourcepawn-mode-*.zip
